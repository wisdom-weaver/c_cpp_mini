node* temp = head;
  // while(temp->next!=head) 
  //   temp = temp->next;
  // node* to_delete = head;
  // temp->next = head->next;
  // head = head->next;
  // display(head);
  // delete to_delete;